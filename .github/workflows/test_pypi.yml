name: Test-PyPI

on:
  workflow_dispatch:
    inputs:
      index:
        description: "Target index: pypi or testpypi"
        required: true
        type: choice
        options:
          - pypi
          - testpypi
        default: testpypi

jobs:
  build-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ['ubuntu-latest']
        # os: ['ubuntu-latest', 'macos-latest']
        # os: ['ubuntu-latest', 'macos-latest', 'windows-latest']
        python-version: ['3.13']
        # python-version: ['3.13', '3.14']
    defaults:
      run:
        shell: bash -l {0}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Dependencies
        run: |
          python -m pip install mypy

      - name: Install PyPi
        if: ${{ inputs.index == 'pypi' }}
        run: |
          python --version
          pip install funasp

      - name: Install PyPi Test
        if: ${{ inputs.index != 'pypi' }}
        run: |
          pip install -i https://test.pypi.org/simple/ funasp

      - name: Remove code
        run: |
          rm -rf funasp/**/*

      - name: Test
        run: |
          nox

      - name: Mypy
        run: |
          nox
